import React from "react";



const Whiteband = (props) => {

    
    return (
        <div className={props.classes}>
                asas
        </div>
    );
}

export default Whiteband;